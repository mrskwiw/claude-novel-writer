# BUGS

Log bugs immediately. Mark complete when resolved. Format: `[YYYY-MM-DD] description — status`

---

[2026-05-29] GAP-01: `assembleSceneContext()` in GenerationManager was a stub returning `{}` — SceneContextAssembler never called — RESOLVED: wired real assembler into constructor, replaced stub with `assembleContext()` call, updated `buildContinuationPrompt` to consume `SceneContext`
[2026-05-29] GAP-02: `defaultModel = 'claude-3-5-sonnet-20241022'` was 18 months stale — RESOLVED: updated to `claude-sonnet-4-6` in `claude-client.ts`
[2026-05-29] GAP-03: `maxTokenBudget` declared in `ContextOptions` but never enforced — RESOLVED: implemented `pruneToTokenBudget()` in `SceneContextAssembler`, called at end of `assembleContext()` with default 8000 token budget
[2026-05-29] PRE-EXISTING: 10 test suites fail due to native module resolution (`yaml/compose/composer.js`, `better-sqlite3/database`) — RESOLVED (stale): vitest.config aliases `yaml`/`better-sqlite3` to their real entry paths; full suite now runs (1376 tests passing as of 2026-06-27)
[2026-06-07] SQL-01: `analyze plot-holes` and `analyze subplots` failed with `no such column: pt.title` — RESOLVED: `pt.title` → `pt.thread_name`; `pb.thread_id` → `pb.plot_thread_id`; `pb.chapter_id` replaced with join through `scenes.chapter_id` in `developmental-analyzer.ts`
[2026-06-07] SQL-02: `analyze scenes` failed with `no such column: s.scene_order` — RESOLVED: `s.scene_order` → `s.scene_number` in `developmental-analyzer.ts`
[2026-06-07] SQL-03: `graph rebuild` failed with `no such column: s.project_id` — RESOLVED: added `JOIN chapters ch ON ch.id = s.chapter_id` and filtered on `ch.project_id` in `narrative-graph-service.ts`
[2026-06-07] CLI-01: `session progress` returned `Unknown subcommand: progress` — RESOLVED: added `progress` as a registered subcommand in `session.ts`
[2026-06-07] UX-01: `character show --name emilie` failed exact-match on Unicode name — RESOLVED: `findCharacterFile` now normalizes NFD and strips combining diacritics, with substring fallback after exact match
[2026-06-27] CODE-01: `create chapter` did not sync to the DB (commented out) and hardcoded chapter number 1 — RESOLVED: auto-numbers via `ChapterSync.getNextChapterNumber()` and calls `syncChapterFile()` after write (`create-handler.ts`)
[2026-06-27] CODE-02: CLI hardcoded `projectId = 1` in `cli/index.ts` — RESOLVED: resolves the real id via `DatabaseManager.getFirstProjectId()` / `NovelWriterExtension.loadProjectId()`
[2026-06-27] CODE-03: `context build` was a stub (Context engine unreachable from CLI) — RESOLVED: `getContextPolicyEngine()` factory wires all fetchers; handler renders assembled context
[2026-06-27] CODE-04: `ChapterSync.generateChapterSummary` wrote placeholder text into `chapters.summary` — RESOLVED: removed from sync; added `GenerationManager.generateChapterSummary` (<=5 sentences) via IClaudeClient passthrough + `generate summary --chapter N`
[2026-06-27] DOC-01: `PLOT_BEAT_SYNC_TODO.md` claimed plot-beat sync was disabled, but it was fixed in SPEC-02 (2026-05-29) — RESOLVED: archived the stale doc to docs/archive/
[2026-06-28] CLI-02: `session`, `location`, and `timeline` handlers each constructed `new NovelWriterExtension(projectPath)` but never resolved the project id, so every DB-backed path threw "Project ID not set" — `session start/end/stats`, `progress`, all `timeline` subcommands, and `location` sync were non-functional. Found during coverage work (session=32%, timeline effectively 0% reachable). — RESOLVED: added `await extension.loadProjectId()` after every extension construction in all three handlers (consistent with the other handlers)
[2026-06-28] CLI-03: `foreshadowCommand` (`commands/foreshadow.ts`) and its handler existed and were tested, but the command was never registered in `registry.ts`, so `/novel foreshadow` was unreachable via the CLI — RESOLVED: imported + registered `foreshadowCommand`
[2026-06-28] SQL-04: `GenerationManager.generateNextSentence` runs `SELECT content FROM scenes WHERE id = ? AND project_id = ?`, but the `scenes` table has neither a `content` nor a `project_id` column, so `/novel generate next-sentence` always throws ("no such column: content") and can never reach its success path — OPEN (needs a design decision: scene prose lives in chapter `.md` files, not the DB; the query/source must be reworked)
[2026-06-28] DATA-01: `CharacterBuilder` writes physical-attribute values unquoted, so a value containing a quote (e.g. `height: 5'6"`) emits invalid YAML that later breaks `syncCharacterFile` ("Unexpected double-quoted-scalar"). Masked because `character-workflow.test.ts` asserts file content but never syncs such values — OPEN (quote/escape attribute values in `character-builder.ts`)
[2026-06-28] CHECK-01: `check characters` / `check timeline` / `check plot-threads` never report issues. They call `checker.getOpenIssues()` (raw snake_case DB rows) then filter on the camelCase `i.issueType`, which is always undefined — RESOLVED: `getOpenIssues()` now maps DB rows to the camelCase `ConsistencyIssue` shape (`checker.ts`); the subcommand filters match and issues are reported
[2026-06-28] CHECK-02: `check list` rendered `[undefined]` badges and blank detail lines — same root cause as CHECK-01 — RESOLVED by the same `getOpenIssues()` mapping fix
[2026-06-28] SQL-05: `world-rule established` could fail with an FK violation — `world-rules-sync.ts` wrote a chapter NUMBER into `established_chapter_id` (FK → `chapters.id`) — RESOLVED: upsert resolves chapter number → chapter id; reverse export resolves id → chapter number
[2026-06-28] DATA-01: `CharacterBuilder` wrote attribute values with a hand-built `"${value}"`, so an embedded `"` produced invalid YAML that broke sync — RESOLVED: added a `dq()` helper that escapes `\` and `"` for YAML double-quoted scalars; normal values render unchanged
[2026-06-28] VAL-01: tension/mood range validations were bypassed by a falsy `0` — RESOLVED: changed the `if (x && …)` guards to `if (x !== undefined && …)` in scene-handler (tension) and session-handler (mood)
[2026-06-28] ROBUST-01: `scene-handler` hardcoded `setProjectId(1)` — RESOLVED: now calls `await extension.loadProjectId()` at all four sites like the other handlers
[2026-06-28] SQL-04: `generate next-sentence` queried `scenes.content`/`scenes.project_id` (neither column exists) and always threw — RESOLVED: removed the broken query; reads the scene's chapter summary via the valid scenes⋈chapters JOIN (scene prose lives in the `.md` files, not the DB)
